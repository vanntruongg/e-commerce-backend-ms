# ORDER-SERVICE
server:
  port: 9006

spring:
  application:
    name: address-data-service
  datasource:
    url: jdbc:postgresql://localhost:5432/address_data
    username: vantruong
    password: vantruong
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        show_sql: true
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

